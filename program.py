code = ('var', 'ret', ';',
    ('proc', 'fib', '(', 'n', ')', '{',
        ('var', 'tmp', ';',
            (';',
                ('=', 'ret', 1),
                (';',
                    ('=', 'tmp', -1),
                    ('while', ('&&', ('!', ('==', 'n', 1)), ('!', ('==', 'n', 2))),
                        ('var', 'local', ';',
                            (';',
                                ('=', 'local', ('-', 'ret', 'tmp')),
                                (';',
                                    ('=', 'tmp', ('-', 'ret')),
                                    (';',
                                        ('=', 'ret', 'local'),
                                        ('=', 'n', ('-', 'n', 1))
                                    )
                                )
                            )
                        )
                    )
                )
            )
        ),

    '}', ';',
        ('proc', 'sum', '(', 'n', ')', '{',
            ('if', ('<', 'n', 1),
                ('skip',),
                (';',
                    ('()', 'sum', ('-', 'n', 1)),
                    ('=', 'ret', ('+', 'ret', 'n'))
                )
            ),
        '}', ';',
            ('if', ('<', 'in', 1),
                ('skip',),
                (';',
                    ('()', 'sum', 'in'),
                    (';',
                        ('()', 'fib', 'ret'),
                        ('print', 'ret')
                    )
                )
            )
        )
    )
)